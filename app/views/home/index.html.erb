<div class="container text-center mt-5">
  <h1 class="display-4">Welcome to Ruby Social</h1>
  <% if account_signed_in? %>
    <div class="mt-5">
      <p class="lead">Visit the dashboard to display your feed:</p>
      <%= link_to "Dashboard", posts_index_path, class: "btn btn-primary mr-2" %>
    </div>

    <div class="mt-3">
      <p class="lead">Visit your profile to look at your posts and edit your account:</p>
      <%= link_to "My Profile", profile_show_path(username: current_account.username), class: "btn btn-secondary mr-2" %>
    </div>

    <div class="mt-3">
      <p class="lead">Go to create a new post here:</p>
      <%= link_to "New Post", new_post_path, class: "btn btn-success mr-2" %>
    </div>
  <% else %>
    <div class="mt-5">
      <p class="lead">To access the features of this website, you will need to sign up or sign in:</p>
      <%= link_to "Login", new_account_session_path, class: "btn btn-primary mr-2" %>
      <%= link_to "Signup", new_account_registration_path, class: "btn btn-secondary mr-2" %>
    </div>
  <% end %>
</div>
